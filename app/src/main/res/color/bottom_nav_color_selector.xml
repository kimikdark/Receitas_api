<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor para o item selecionado (o nosso castanho quente) -->
    <item android:color="@color/primary_dark" android:state_checked="true" />
    <!-- Cor para o item não selecionado (um cinza neutro) -->
    <item android:color="@android:color/darker_gray" android:state_checked="false" />
</selector>
